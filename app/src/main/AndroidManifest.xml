<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="kr.ac.gachon.parking">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.READ_PROFILE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="1d57e58a63f303253a2081f1a0ce9146"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="parking"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".Info.HolidayInfo">
        </activity>
        <activity android:name=".Info.DisabledInfo">
        </activity>

        <meta-data
                android:name="com.naver.maps.map.CLIENT_ID"
                android:value="cc86tt11qz"/>

        <activity android:name=".Customer.MyInfoActivity">
        </activity>
        <activity android:name=".Group.MyGroupActivity">
        </activity>
        <activity android:name=".Group.GroupMakingActivity">
        </activity>
        <activity android:name=".Group.GroupActivity">
        </activity>
        <activity android:name=".Customer.SignupActivity">
        </activity>
        <activity
                android:name=".Customer.LoginActivity"
                android:label="@string/title_activity_login">
        </activity>
        <activity
                android:name=".MainActivity"
                android:label="@string/title_activity_main"
                android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>